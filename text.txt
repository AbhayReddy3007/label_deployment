SELECT
    Cleaned_Generic_Name,
    STRING_AGG(DISTINCT Mechanism_of_Action, '; ') AS Mechanisms_Of_Action
FROM `cognito-dev-380506.data_mart.drug_details`
WHERE Cleaned_Generic_Name = 'Tirzepatide'
GROUP BY Cleaned_Generic_Name;
